# Clippy Configuration

# Deny panicking functions in production code
# This forces proper error handling
disallowed-methods = [
    { path = "std::option::Option::expect", reason = "Use ? or map_err instead of panicking" },
    { path = "std::result::Result::expect", reason = "Use ? or map_err instead of panicking" },
    { path = "std::option::Option::unwrap", reason = "Use ? or map_err instead of panicking" },
    { path = "std::result::Result::unwrap", reason = "Use ? or map_err instead of panicking" },
]
